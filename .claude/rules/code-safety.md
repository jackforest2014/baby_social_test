---
description: 代码安全规则
globs: "**/*.{go,ts,tsx,js,jsx,sql}"
---

# 代码安全规则

- 禁止在代码中硬编码密码、API Key、Token 等敏感信息，必须使用环境变量
- SQL 查询必须使用参数化查询，禁止字符串拼接
- 用户输入必须进行验证和转义，防止 XSS 和注入攻击
- API 接口必须进行认证和授权检查
- 敏感数据（如密码）必须加密存储
